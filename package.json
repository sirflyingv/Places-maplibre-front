{
  "name": "places-maplibre-front",
  "version": "0.5.0",
  "description": "frontend for places project to work with places API",
  "scripts": {
    "start": "parcel ./src/html/index.html",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "sirflyingv",
  "license": "ISC",
  "dependencies": {
    "maplibre-gl": "^2.4.0",
    "parcel": "^2.7.0"
  },
  "devDependencies": {
    "eslint": "^8.25.0"
  }
}
